class A {
    int x = 1;
    String str = "hello";
    boolean emptyStr = false;

    void foo() {
        while(emptyStr || emptyStr && emptyStr) {
            break;
        }
    }

    void main() {
        var y = 2;
    }
}

class B {
    String myString = "Class B!";
}